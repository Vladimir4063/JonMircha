//ingrese valore enteros en una arreglo, y muestre por consola el numero mayor

lista = [1, 3, 4, 7, 5, 6, 2, 65, 32, 86, 22, 11, 45];

for (i in lista) {
  num = 0;
  if (lista[i] > num) {
    num = lista[i];
    //console.log(num);
  }
  console.log(num);
}
